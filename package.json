{
  "name": "mini-trello",
  "version": "1.0.0",
  "description": "트렐로의 최소한 기능을 담은 미니 트렐로",
  "main": "./src/server",
  "scripts": {
    "build": "tsc",
    "start": "npm run build & node dist/server.js",
    "dev": "nodemon --watch \"src/**/*.ts\" --exec \"ts-node\" src/server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Minho.Kwon",
  "license": "ISC",
  "dependencies": {
    "@types/express": "^4.17.13",
    "express": "^4.17.3"
  },
  "devDependencies": {
    "nodemon": "^2.0.15",
    "ts-node": "^10.5.0"
  }
}
